[project]
name = "service-dashboard"
version = "0.1.0"
description = "Transaction monitoring dashboard"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "apscheduler",
    "jinja2",
]
